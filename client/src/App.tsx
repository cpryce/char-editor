function App() {
  return (
    <main className="p-8">
      <h1 className="text-3xl font-semibold" style={{ color: 'var(--color-fg-default)' }}>
        char-editor
      </h1>
      <p style={{ color: 'var(--color-fg-muted)' }} className="mt-2 text-sm">
        React + Vite + Tailwind + Base UI · GitHub theme
      </p>
    </main>
  )
}

export default App
